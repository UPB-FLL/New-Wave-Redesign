import { CurrentField } from '../brand/CurrentField';

export default function VideoBackground() {
  return <CurrentField density="standard" tone="dark" className="absolute inset-0 h-full w-full opacity-65" aria-hidden="true" />;
}
